# 复审聚焦指南

本轮为复审——上一轮审查有必须改项未通过。

## 策略

跳过第一轮全量扫描。直接从第二轮（质疑）开始，重点复盘上一轮标记的 `## 必须改` 项。

## 检查重点

1. 上一轮必须改项是否真的改掉了？（不是假装改掉了——验证具体代码变更）
2. 修复有没有引入新问题？（查相邻模块和接口）
3. 上一轮的建议改项有没有被连带恶化？
4. 收敛判据中"本轮建议改 ≤ 上轮建议改"是否成立？

## 铁律

不要全量重扫。聚焦在上一轮审查报告中标记过问题的区域。第三轮（改进追问）照常执行。
