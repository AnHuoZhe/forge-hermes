# 中审标准

当前模块所有任务完成并锁定后执行。读取模块代码、相关测试和架构文档。

## 检查清单

1. **模块内协作**：所有零件组合后接口对接和控制流不冲突。
2. **I/O 正确**：给定已知测试输入，模块整体输出符合架构契约和预期错误行为。
3. **不越层**：依赖只指向本层或下层，不绕过边界直接访问上层或无关模块。
4. **测试覆盖**：核心逻辑和模块协作路径有测试锁住。

## 边界

不重复小审已经覆盖的代码级接口签名、文件位置和安全性检查。聚焦模块协作、数据流和架构一致性。
